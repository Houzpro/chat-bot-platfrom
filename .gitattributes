*.gguf filter=lfs diff=lfs merge=lfs -text

# Ensure shell scripts always have LF line endings (critical for Docker on Windows)
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
